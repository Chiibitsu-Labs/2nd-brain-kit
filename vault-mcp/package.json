{
  "name": "aikiri-vault-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Remote MCP server exposing read/write access to your Obsidian vault via the GitHub API, so Claude (web/mobile/Code) and ChatGPT Business can reach your notes without a local Obsidian instance or file sync. Part of the Second Brain Kit by Chiibitsu Labs.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "OAUTH_SIGNING_SECRET=$(node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\") tsx lib/oauth.test.ts"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "mcp-handler": "^1.0.2",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "tsx": "^4.23.12",
    "typescript": "^5.7.0"
  }
}
